#include <stdio.h>

int main(void)
{
    // char ch = 9;
    // int inum = 1052;
    // double dnum = 3.1415;
    // printf("The size of variable ch is %d \n", sizeof(ch));
    // printf("The size of variable inum is %d \n", sizeof(inum));
    // printf("The size of variable dnum is %d \n\n", sizeof(dnum));

    // printf("The size of char : %d \n", sizeof(char));
    // printf("The size of int : %d \n", sizeof(int));
    // printf("The size of long : %d \n", sizeof(long));
    // printf("The size of long long : %d \n", sizeof(long long));
    // printf("The size of float : %d \n", sizeof(float));
    // printf("The size of double : %d \n\n", sizeof(double));
    

    // 원의 반지름 계산
    // double rad;
    // double area;
    // printf("Type the radius of the circle : ");
    // scanf("%lf", &rad);
    // area = rad * rad * 3.1415;
    // printf("The width of the circle : %.2f \n", area);


    // 118p question
    // 01
    int x, y, X, Y;
    // printf("좌상단 좌표를 입력해주세요 : ");
    // scanf("%d %d", &x, &y);
    // printf("우하단 좌표를 입력해주세요 : ");
    // scanf("%d %d", &X, &Y);
    // printf("좌상단의 x, y 좌표 : %d %d\n우하단의 x, y 좌표 : %d %d\n", x, y, X, Y);
    // printf("두 점이 이루는 사각형의 넓이 : %d\n\n", ((X-x)*(Y-y)));

    // //02
    // double num1, num2, result1;
    // printf("Type the two real number : ");
    // scanf("%lf %lf", &num1, &num2);
    // result1 = num1 + num2 * (num1/num2);
    // printf("The calculation result : %.2f\n\n", result1);

    // //04
    // int str1;
    // printf("Type the integer num (0~127) : ");
    // scanf("%d", &str1);
    // printf("The ASCII value of the number : %c\n\n", str1);

    // //05
    // char str2;
    // printf("Type the one letter : ");
    // scanf(" %c", &str2); // 공백이 없으면 위에서 친 엔터를 인식
    // printf("The ASCII value of the letter : %d\n\n", str2);


    

    return 0;
}