#include <stdio.h>

int main(void)
{
    // int a,b,c; 
    // int quit = 1;

    // for(c = 3; c <= 997; c++) // 더 최적화 시킬 수는 없을까...?
    // {
    //     if(quit == 0)
    //     {    
    //         break;
    //     }
    //     for(b = 2; b < c; b++)
    //     {
    //         if(quit == 0)
    //         {    
    //             break;
    //         }
    //         for(a = 1; a < b; a++)
    //             {
    //                 if((a*a+b*b == c*c) && (a + b + c == 1000))
    //                 {
    //                     printf("합이 1000인 피타고라스 함수는 %d, %d, %d이고 셋의 곱은 %d 입니다\n\n", a, b, c, a*b*c);
    //                     quit = 0; // 답이 1개밖에 없다는 사실을 알기 때문에 연산을 끝냈다...
    //                     break;
    //                 }
    //             }
    //     }
    // }

    return 0;
}